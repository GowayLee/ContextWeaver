---
phase: 02.1-contextweaver-cw
plan: 02
subsystem: cli
tags: [cac, nodejs, typescript, cli-entry]
requires:
  - phase: 02.1-01
    provides: dist 入口 smoke 回归与共享入口失败重现
provides:
  - 共享 CLI 入口参数规范化
  - 主模块显式 runCli 启动路径
  - 无参数与 help 别名统一帮助输出
affects: [contextweaver, cw, dist/index.js, cli-help]
tech-stack:
  added: []
  patterns: [shared argv normalization, explicit main-module runner]
key-files:
  created: [.planning/phases/02.1-contextweaver-cw/02.1-02-SUMMARY.md]
  modified: [src/index.ts]
key-decisions:
  - "在 src/index.ts 入口边界统一把 [] 与 help 归一化为 --help，而不是给单个命令打补丁。"
  - "主模块分支改为调用 runCli，并显式把规范化后的 argv 传给 cli.parse(...)。"
patterns-established:
  - "CLI shared entry: 入口层先规范化 argv，再调用 cac parse。"
  - "Import-safe bootstrap: 仅主模块分支执行 runCli，测试导入不触发 parse。"
requirements-completed: [TBD]
duration: 5min
completed: 2026-04-01
---

# Phase 02.1 Plan 02: Shared CLI Entry Summary

**共享 CLI 入口现在会把无参数和 help 统一映射到标准帮助输出，并通过显式 runCli 保持 contextweaver/cw/dist 入口派发一致。**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-01T01:40:00Z
- **Completed:** 2026-04-01T01:45:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- 在 `src/index.ts` 新增 `normalizeCliArgs()`，把裸调用和 `help` 统一归一化到 `--help`
- 保留 `-v` / `--version` 快捷路径，避免被默认帮助逻辑吞掉
- 用 `runCli()` 接管主模块启动，并显式把规范化后的 argv 传给 `cli.parse(...)`

## Task Commits

Each task was committed atomically:

1. **Task 1: Normalize bare invocation and explicit `help` at the shared argv boundary** - `15b9072` (feat)
2. **Task 2: Route the main-module entry through normalized parse without breaking existing command dispatch** - `b43cb5c` (refactor)

**Plan metadata:** pending

## Files Created/Modified

- `src/index.ts` - 新增共享 argv 规范化 helper，并把主模块启动切到 `runCli()`
- `.planning/phases/02.1-contextweaver-cw/02.1-02-SUMMARY.md` - 记录本次计划执行、验证与决策

## Decisions Made

- 用入口层参数规范化修复 help 契约，避免在 `index`、`search`、`prompt-context` action 内做分散补丁
- 继续使用现有 `cac` 实例与命令注册，只替换最终 parse 调用路径，降低 brownfield 回归风险

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `pnpm test -- tests/entryCli.test.ts tests/indexCli.test.ts` 实际会执行整个 Vitest 套件；虽然超出目标文件范围，但本次运行全部通过，未阻塞计划完成

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CLI 共享入口契约已稳定，可继续推进后续索引可靠性阶段
- 当前未发现需要在 `README` 或子命令层追加补丁的问题

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-contextweaver-cw/02.1-02-SUMMARY.md`
- FOUND: `15b9072`
- FOUND: `b43cb5c`

---

_Phase: 02.1-contextweaver-cw_
_Completed: 2026-04-01_
