---
phase: 02.1-contextweaver-cw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/entryCli.test.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - Running `node dist/index.js` with no arguments prints standard CLI help and exits with code 0.
    - `help`, `-h`, and `--help` all produce the same help surface for the shared CLI entrypoint.
    - Representative entry invocations such as `index --help` and `search` produce visible output instead of a silent exit.
    - Package binaries `contextweaver` and `cw` still point at the same built entry file.
  artifacts:
    - path: tests/entryCli.test.ts
      provides: Subprocess smoke coverage for default help, help aliases, alias wrappers, and representative dispatch visibility.
  key_links:
    - from: tests/entryCli.test.ts
      to: dist/index.js
      via: spawned CLI subprocesses that assert stdout/stderr and exit code.
      pattern: dist/index.js|Usage:|缺少 --information-request
    - from: tests/entryCli.test.ts
      to: package.json
      via: bin mapping assertions for `contextweaver` and `cw`.
      pattern: '"contextweaver"|"cw"|dist/index.js'
---

<objective>
Lock the urgent CLI entry regression with executable smoke tests before changing the shared entrypoint.

Purpose: implement D-01 through D-07 as failing-first regression coverage so the fix targets the shared entry chain instead of ad-hoc subcommand patches.
Output: a dedicated subprocess smoke suite covering no-arg help, explicit help aliases, bin alias parity, and representative subcommand visibility.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-contextweaver-cw/02.1-CONTEXT.md
@.planning/phases/01-fail-fast-exit/01-02-SUMMARY.md
@.planning/phases/02-provider-diagnostics/02-02-SUMMARY.md
@package.json
@README.md
@src/index.ts
@tests/indexCli.test.ts

<interfaces>
Current package entry contract from `package.json`:

```json
{
  "bin": {
    "contextweaver": "dist/index.js",
    "cw": "dist/index.js"
  }
}
```

Current runtime tail in `src/index.ts`:

```ts
cli.help();

if (
  process.argv[1] &&
  path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)
) {
  cli.parse();
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subprocess smoke tests for no-arg help and explicit help aliases</name>
  <read_first>
    tests/indexCli.test.ts
    src/index.ts
    package.json
    .planning/phases/02.1-contextweaver-cw/02.1-CONTEXT.md
  </read_first>
  <files>tests/entryCli.test.ts</files>
  <action>新增 `tests/entryCli.test.ts`，使用 Node 内置 `spawnSync` 或 `execFileSync` 启动 `process.execPath` + `dist/index.js` 做真实子进程 smoke test。先写一个小的 `runEntry(args: string[])` helper，固定 `cwd` 为仓库根目录、`encoding: 'utf-8'`，并把 `stdout` 与 `stderr` 合并成可断言字符串。覆盖这组契约：① `[]` 必须退出码 `0`，输出包含 `Usage:`、`Commands:`、`contextweaver`，且不是空字符串（实现 D-01、D-02）；② `['help']`、`['-h']`、`['--help']` 都必须退出码 `0`，并与无参数帮助至少共享 `Usage:`、`Commands:`、`Options:` 这些标准段落（实现 D-03）；③ 保留现有 `-v` / `--version` 单独输出版本号行为，不要让默认 help 修复破坏版本快捷路径。</action>
  <acceptance_criteria>
    - `tests/entryCli.test.ts` contains a subprocess helper that executes `dist/index.js` via `process.execPath`.
    - `tests/entryCli.test.ts` asserts `[]`, `help`, `-h`, and `--help` all exit with code `0` and produce non-empty help output.
    - `tests/entryCli.test.ts` contains explicit expectations for `Usage:`, `Commands:`, and `Options:` in help output.
    - `tests/entryCli.test.ts` preserves a version shortcut assertion for `-v` or `--version`.
    - `pnpm build && pnpm test -- tests/entryCli.test.ts` fails before the entrypoint fix and passes after Plan 02.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm test -- tests/entryCli.test.ts</automated>
  </verify>
  <done>The no-arg and help-alias CLI contract is captured in executable smoke coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add alias parity and representative dispatch visibility smoke coverage</name>
  <read_first>
    tests/entryCli.test.ts
    package.json
    README.md
    src/index.ts
    .planning/phases/02.1-contextweaver-cw/02.1-CONTEXT.md
  </read_first>
  <files>tests/entryCli.test.ts</files>
  <action>继续扩展 `tests/entryCli.test.ts`，把 Phase 02.1 的共享入口范围锁死。先读取 `package.json` 并断言 `bin.contextweaver === 'dist/index.js'` 且 `bin.cw === 'dist/index.js'`，实现 D-07 的发布入口约束。然后在临时目录里创建两个可执行别名包装：`contextweaver` 与 `cw` 都指向仓库里的 `dist/index.js`（可用 symlink 或复制文件后 `chmod 0o755`），分别执行无参数和 `--help`，断言两者 stdout 完全一致且退出码都为 `0`。最后加两个代表性派发 smoke case：`['index', '--help']` 必须输出 `Usage:` 和 `--force`，`['search']` 必须以非零退出并输出 `缺少 --information-request`，证明 D-04/D-05 要求的共享入口派发仍有可见反馈，而不是静默结束。</action>
  <acceptance_criteria>
    - `tests/entryCli.test.ts` asserts both `package.json` bin aliases point to `dist/index.js`.
    - `tests/entryCli.test.ts` contains executable alias-wrapper coverage for both `contextweaver` and `cw`.
    - `tests/entryCli.test.ts` asserts `contextweaver` and `cw` produce identical no-arg and `--help` output.
    - `tests/entryCli.test.ts` asserts `index --help` shows `--force` and `search` shows `缺少 --information-request`.
    - `pnpm build && pnpm test -- tests/entryCli.test.ts` exits `0` after Plan 02 implementation.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm test -- tests/entryCli.test.ts</automated>
  </verify>
  <done>The release entry aliases and representative shared-entry dispatch behaviors are protected by smoke tests.</done>
</task>

</tasks>

<verification>
- `pnpm build && pnpm test -- tests/entryCli.test.ts`
</verification>

<success_criteria>

- `contextweaver` / `cw` no longer rely on manual checking for default help behavior.
- Help aliases and representative subcommands are covered at the built-artifact boundary, not just by source-level assumptions.
- The release bin contract (`contextweaver` and `cw` both targeting `dist/index.js`) is locked by tests.
  </success_criteria>

<output>
After completion, create `.planning/phases/02.1-contextweaver-cw/02.1-01-SUMMARY.md`
</output>
