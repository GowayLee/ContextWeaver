---
phase: 02.1-contextweaver-cw
plan: 01
subsystem: testing
tags: [cli, cac, vitest, smoke-test, dist-entry]
requires:
  - phase: 02-provider-diagnostics
    provides: 顶层 CLI 边界与现有入口行为基线
provides:
  - 基于 dist/index.js 的 CLI 入口 smoke 回归测试
  - contextweaver/cw 别名一致性与代表性派发可见性断言
affects: [02.1-02, cli-entry, release-bin]
tech-stack:
  added: []
  patterns: [子进程级 dist 入口回归测试, 发布别名包装器一致性校验]
key-files:
  created: [tests/entryCli.test.ts]
  modified: []
key-decisions:
  - "直接针对 dist/index.js 跑子进程 smoke test，避免只验证源码层假设。"
  - "用临时包装器模拟 contextweaver 与 cw 两个发布别名，锁定共享入口的一致性。"
patterns-established:
  - "CLI 入口回归优先覆盖真实构建产物与退出码。"
  - "代表性子命令至少要验证有可见输出，而不是只测 help 文本。"
requirements-completed: []
duration: 15min
completed: 2026-04-01
---

# Phase 02.1 Plan 01: CLI entry smoke regression Summary

**为 dist/index.js 增加真实子进程 smoke 回归，锁定默认 help、帮助别名、发布别名一致性与代表性子命令输出契约。**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-01T01:24:00Z
- **Completed:** 2026-04-01T01:39:46Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- 新增 `tests/entryCli.test.ts`，直接通过 `process.execPath + dist/index.js` 验证无参数与 help 别名行为。
- 锁定 `contextweaver` / `cw` 两个发布别名仍指向同一 `dist/index.js`，并通过临时包装器验证输出一致。
- 覆盖 `index --help` 与 `search` 两个代表性入口派发场景，证明当前回归集中在默认 help 输出失声。

## Task Commits

Each task was committed atomically:

1. **Task 1: Add subprocess smoke tests for no-arg help and explicit help aliases** - `291b963` (test)
2. **Task 2: Add alias parity and representative dispatch visibility smoke coverage** - `46789f8` (test)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `tests/entryCli.test.ts` - 子进程级 CLI 入口 smoke 测试，覆盖默认 help、help 别名、版本快捷路径、发布别名与代表性派发。

## Decisions Made

- 直接读取 `package.json` 并断言 `bin.contextweaver` / `bin.cw` 都指向 `dist/index.js`，把发布入口契约纳入回归保护。
- 让验证命令保持红灯是本计划预期结果，因为 Plan 01 的目标是先把回归复现并固定在测试里，真正修复留给 02.1-02。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 手动修正 STATE.md 到已完成 02.1-01 的磁盘事实**

- **Found during:** Plan wrap-up / state update
- **Issue:** `state advance-plan` 无法解析旧格式 `STATE.md` 的 Current Plan 区块，导致当前计划位置没有自动推进。
- **Fix:** 保留 gsd-tools 已写入的指标与 session 信息，并手动把 Current Position、进度条和累计指标改到 `02.1-01` 已完成后的状态。
- **Files modified:** `.planning/STATE.md`
- **Verification:** `STATE.md` 已显示 `Plan: 02`、`83%` 进度和新增的 `Phase 02.1 P01` 指标记录。
- **Committed in:** pending final docs commit

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** 仅修正执行元数据，未改变代码与测试范围。

## Issues Encountered

- `pnpm build && pnpm test -- tests/entryCli.test.ts` 仍失败，但失败点稳定且符合预期：无参数与 help 别名输出为空字符串，正是本 phase 要在下一个计划修复的共享入口回归。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 已有稳定红灯回归基线，可直接在共享入口修复默认 help 与 parse 契约。
- 当前阻塞仅剩代码修复本身；测试、构建与回归定位信息已准备好。

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-contextweaver-cw/02.1-01-SUMMARY.md`
- FOUND: `291b963`
- FOUND: `46789f8`

---

_Phase: 02.1-contextweaver-cw_
_Completed: 2026-04-01_
