# Phase 02.1: contextweaver-cw - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01T00:00:00.000Z
**Phase:** 02.1-contextweaver-cw
**Areas discussed:** 默认启动行为, 退出码语义, 回归保护范围, 适用范围, 验收范围

---

## 默认启动行为

| Option       | Description                                 | Selected |
| ------------ | ------------------------------------------- | -------- |
| 完整 help    | 直接显示标准 help/usage、命令列表和常用选项 | ✓        |
| 精简用法     | 只显示一小段 usage + 常用命令               |          |
| 自定义欢迎页 | 显示项目介绍或 quick start 风格文案         |          |

**User's choice:** 完整 help
**Notes:** 用户希望裸跑 `contextweaver` / `cw` 时直接看到完整 help，而不是静默无输出。

---

## 退出码语义

| Option   | Description                      | Selected |
| -------- | -------------------------------- | -------- |
| 返回 0   | 无参数展示 help 视为正常帮助展示 | ✓        |
| 返回非 0 | 把无参数视为误用                 |          |

**User's choice:** 返回 0
**Notes:** 用户将这类场景定义为正常 CLI 帮助展示，不应表现成失败。

---

## 回归保护范围

| Option       | Description                            | Selected |
| ------------ | -------------------------------------- | -------- |
| 入口全覆盖   | 覆盖别名、help 族与构建产物 smoke test | ✓        |
| 只测源码入口 | 只补源码入口测试                       |          |
| 只修不补测试 | 先修行为，不新增测试                   |          |

**User's choice:** 入口全覆盖
**Notes:** 用户明确要求防止“源码修了但发布入口继续坏”的回归。

---

## 适用范围

| Option              | Description                              | Selected |
| ------------------- | ---------------------------------------- | -------- |
| 修整个 CLI 入口链路 | 无参数、help、代表性子命令都恢复可见输出 | ✓        |
| 先只修 help 与裸跑  | 先恢复帮助输出                           |          |
| 只保底核心子命令    | 覆盖无参数、`index`、`search`，其他后置  |          |

**User's choice:** 修整个 CLI 入口链路
**Notes:** 用户补充关键信息：不仅裸跑无输出，`index` / `search` 也无输出，因此 phase 边界上调为整体入口链路失声修复。

---

## 验收范围

| Option       | Description                                                     | Selected |
| ------------ | --------------------------------------------------------------- | -------- |
| 全用户入口   | 无参数、help、`-h`、`--help`、`index`、`search`，并覆盖两个别名 | ✓        |
| 只测核心路径 | 无参数、`index`、`search` 三条主路径                            |          |
| 你来定       | 由实现阶段选择最小集合                                          |          |

**User's choice:** 全用户入口
**Notes:** 用户希望把所有主要用户触点都锁进验收，降低发布后再次静默失效的概率。

---

## the agent's Discretion

- help 的具体排版与 `cac` 接口选型由实现阶段决定。
- 测试采用何种组合方式由实现阶段决定，只要能覆盖入口链路与发布产物即可。

## Deferred Ideas

None.
