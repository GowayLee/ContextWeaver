---
phase: 02.1-contextweaver-cw
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - src/index.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - Running `contextweaver` or `cw` with no arguments shows full standard help and exits with code 0.
    - `help`, `-h`, and `--help` behave consistently at the shared entry boundary.
    - Representative commands such as `index --help` and `search` still dispatch through the shared entry and produce visible output.
  artifacts:
    - path: src/index.ts
      provides: Shared CLI entry normalization and main-module execution wiring.
  key_links:
    - from: src/index.ts
      to: cac parser
      via: normalized argv passed into `cli.parse(...)` before command dispatch.
      pattern: cli.parse|help|--help
    - from: src/index.ts
      to: terminal output
      via: no-arg normalization into help output plus unchanged version and subcommand actions.
      pattern: -v|--version|Usage:|缺少 --information-request
---

<objective>
Repair the shared CLI entrypoint so the built binary stops silently exiting on bare invocation while preserving existing subcommand behavior.

Purpose: implement D-01 through D-05 by fixing the single runtime entry boundary in `src/index.ts`, not by patching individual commands.
Output: a normalized CLI bootstrap path that turns bare invocation into standard help, keeps help aliases consistent, and leaves current command actions intact.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-contextweaver-cw/02.1-CONTEXT.md
@.planning/phases/01-fail-fast-exit/01-02-SUMMARY.md
@.planning/phases/02-provider-diagnostics/02-02-SUMMARY.md
@.planning/phases/02.1-contextweaver-cw/02.1-01-PLAN.md
@package.json
@README.md
@src/index.ts
@tests/entryCli.test.ts
@tests/indexCli.test.ts

<interfaces>
Existing command-specific helper in `src/index.ts` that must keep working:

```ts
export async function runIndexCliCommand(options: {
  rootPath: string;
  force?: boolean;
  yes?: boolean;
  isInteractive?: boolean;
  runIndexCommandFn?: typeof runIndexCommand;
  logger?: {
    info: (message: string) => void;
    error: (message: string) => void;
  };
  exit?: (code: number) => void;
}): Promise<void>;
```

Current shared-entry tail to replace safely:

```ts
cli.help();

if (
  process.argv[1] &&
  path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)
) {
  cli.parse();
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize bare invocation and explicit `help` at the shared argv boundary</name>
  <read_first>
    src/index.ts
    tests/entryCli.test.ts
    package.json
    .planning/phases/02.1-contextweaver-cw/02.1-CONTEXT.md
  </read_first>
  <files>src/index.ts</files>
  <behavior>
    - Bare invocation `[]` is normalized to the same help path as `--help`, per D-01 and D-02.
    - The explicit `help` token is normalized to `--help`, per D-03.
    - `-v` / `--version` still bypass help normalization and print version only.
  </behavior>
  <action>在 `src/index.ts` 的共享入口层新增一个小的纯 helper，例如 `normalizeCliArgs(argv: string[]): string[]`。规则必须写死为：`[] -> ['--help']`、`['help'] -> ['--help']`、`['-h'] -> ['-h']`、`['--help'] -> ['--help']`，其余参数数组原样返回。保留现有 `-v` / `--version` 的快捷路径在前面运行，保证版本命令仍只输出版本号且退出 `0`，不要被默认 help 逻辑吞掉。不要在 `index`、`search`、`prompt-context` 等单个命令 action 里打补丁，修复点必须停留在共享入口边界，符合 D-05。</action>
  <acceptance_criteria>
    - `src/index.ts` contains a dedicated argv-normalization helper for `[]` and `help`.
    - `src/index.ts` maps bare invocation to `--help` without changing non-help subcommand argv.
    - `src/index.ts` preserves the existing `-v` / `--version` early-return behavior.
    - `pnpm build && pnpm test -- tests/entryCli.test.ts tests/indexCli.test.ts` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm test -- tests/entryCli.test.ts tests/indexCli.test.ts</automated>
  </verify>
  <done>The shared CLI entrypoint now has an explicit normalization contract for bare invocation and `help`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route the main-module entry through normalized parse without breaking existing command dispatch</name>
  <read_first>
    src/index.ts
    tests/entryCli.test.ts
    tests/indexCli.test.ts
    README.md
    .planning/phases/02.1-contextweaver-cw/02.1-01-PLAN.md
  </read_first>
  <files>src/index.ts</files>
  <behavior>
    - Direct execution of `dist/index.js` uses normalized argv before `cac` parse.
    - Importing `src/index.ts` for tests still does not auto-run CLI parse.
    - Existing subcommand wiring for `index`, `search`, and `prompt-context` remains unchanged and visible.
  </behavior>
  <action>把 `src/index.ts` 的底部启动逻辑改成显式的共享入口 runner，例如 `runCli(argv = process.argv.slice(2), invokedPath = process.argv[1])`。在这个 runner 里继续复用现有 `cli.help()` 和所有命令注册，只把最终解析调用改为基于规范化参数的显式 argv 数组，例如 `[process.execPath, invokedPath ?? 'contextweaver', ...normalizeCliArgs(argv)]` 再传给 `cli.parse(...)`。主模块判断仍然使用 `path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)`，保证测试导入 `src/index.ts` 时不会自动执行 parse；但一旦是直接执行 built entry，就一定走新的 normalized parse 路径。不要改变 `runIndexCliCommand()`、`search` 缺参报错、`prompt-context` 参数解析或现有 logger 输出语义，只修复“入口 silent on no args”的共享链路问题。</action>
  <acceptance_criteria>
    - `src/index.ts` main-module branch calls a shared runner instead of bare `cli.parse()`.
    - `src/index.ts` passes an explicit normalized argv array into `cli.parse(...)`.
    - `src/index.ts` keeps `runIndexCliCommand()` and existing subcommand actions intact.
    - `pnpm build && pnpm test -- tests/entryCli.test.ts tests/indexCli.test.ts` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm test -- tests/entryCli.test.ts tests/indexCli.test.ts</automated>
  </verify>
  <done>The built CLI now shows help on bare invocation, while alias parity and representative command dispatch continue to work through the same entry file.</done>
</task>

</tasks>

<verification>
- `pnpm build && pnpm test -- tests/entryCli.test.ts tests/indexCli.test.ts`
- `node dist/index.js`
- `node dist/index.js search`
</verification>

<success_criteria>

- `contextweaver` / `cw` no-arg invocation no longer exits silently.
- All help trigger forms use one shared entry normalization path.
- Representative subcommands still produce visible output through the same built entrypoint.
  </success_criteria>

<output>
After completion, create `.planning/phases/02.1-contextweaver-cw/02.1-02-SUMMARY.md`
</output>
