---
phase: 03-index-visibility
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/indexVisibilitySummary.test.ts
  - src/index.ts
autonomous: true
requirements:
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - Success and failure use clearly separate final templates.
    - Final output shows skipped totals with reason buckets when skips occurred.
    - Edge cases like no changes, delete/self-heal only, and pre-embed failure get explicit conclusion lines.
  artifacts:
    - path: tests/indexVisibilitySummary.test.ts
      provides: Regression coverage for honest success/failure summaries and edge-case wording.
    - path: src/index.ts
      provides: Final summary renderers using partial stats and skip buckets.
  key_links:
    - from: src/index.ts
      to: src/scanner/index.ts
      via: success/failure renderer consumes `skippedByReason`, `visibility`, and typed `stage` metadata.
      pattern: skippedByReason|partialStats|失败阶段
    - from: src/index.ts
      to: src/api/embedding.ts
      via: Phase 3 failure summary preserves Phase 2 diagnostics block after the honest verdict.
      pattern: formatEmbeddingFailureDiagnostics|索引失败
---

<objective>
Render honest final summaries and explicit edge-case conclusions at the CLI boundary.

Purpose: implement D-07 through D-11 so VIS-03 is true and VIS-02 becomes visible in the final summary without breaking Phase 1/2 failure semantics.
Output: regression tests plus final CLI renderers for separate success/failure templates, skip-reason distribution, and specialized edge-case conclusions.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-index-visibility/03-CONTEXT.md
@.planning/phases/03-index-visibility/03-RESEARCH.md
@.planning/phases/03-index-visibility/03-VALIDATION.md
@.planning/phases/03-index-visibility/03-01-PLAN.md
@.planning/phases/02-provider-diagnostics/02-02-SUMMARY.md

<interfaces>
From earlier phases and 03-01 foundation:

```ts
export interface ScanStats {
  totalFiles: number;
  added: number;
  modified: number;
  unchanged: number;
  deleted: number;
  skipped: number;
  errors: number;
  skippedByReason: Partial<Record<SkipReasonBucket, number>>;
  visibility: {
    candidateFiles: number;
    processedFiles: number;
    embeddingFiles: number;
    selfHealFiles: number;
    deletedPaths: number;
  };
  vectorIndex?: {
    indexed: number;
    deleted: number;
    errors: number;
  };
}

export class ScanStageError extends Error {
  readonly stage: IndexStage;
  readonly partialStats?: ScanStats;
}

export class EmbeddingFatalError extends Error {
  readonly diagnostics?: EmbeddingFailureDiagnostics;
}
```

Current CLI boundary to extend:

```ts
export async function runIndexCliCommand(options: {
  rootPath: string;
  force?: boolean;
  yes?: boolean;
  isInteractive?: boolean;
  runIndexCommandFn?: typeof runIndexCommand;
  logger?: {
    info: (message: string) => void;
    error: (message: string) => void;
  };
  exit?: (code: number) => void;
}): Promise<void>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI summary tests for honest verdicts, skip distributions, and edge cases</name>
  <read_first>
    src/index.ts
    tests/indexCli.test.ts
    .planning/phases/03-index-visibility/03-CONTEXT.md
    .planning/phases/03-index-visibility/03-RESEARCH.md
    .planning/phases/02-provider-diagnostics/02-02-SUMMARY.md
  </read_first>
  <files>tests/indexVisibilitySummary.test.ts</files>
  <behavior>
    - Test 1: per D-07, success and failure endings use separate templates and failure never prints success-only wording.
    - Test 2: per D-04 and D-06, success summary prints skipped total plus reason buckets, without default per-file skipped paths.
    - Test 3: per D-10 and D-11, no-change, delete/self-heal-only, and pre-embed-failure cases each get an explicit conclusion line.
  </behavior>
  <action>创建 `tests/indexVisibilitySummary.test.ts`，围绕 `runIndexCliCommand()` 增加三组断言。第一组：成功返回带 `skippedByReason` 的 `ScanStats` 时，输出包含 `索引完成：...` 专门成功结论、保留数值统计，并新增一行 `跳过原因:`，其中至少覆盖 `large_file`、`binary_file`、`ignored_json`、`no_indexable_chunks`、`processing_error` 的中文标签映射；同时断言输出里没有默认逐文件 skipped path 列表（per D-06）。第二组：失败返回 `ScanStageError` 或等价错误时，第一行必须是 `索引失败：...` 或 `索引失败: ...`，随后有 `失败阶段:` 和 `已知统计:`，且绝不能出现成功模板。第三组：为三类边界场景锁定专门结论：`索引完成：没有检测到新的可索引变更`、`索引完成：已同步删除或自愈，无新增向量嵌入`、`索引失败：在 process 阶段终止`（stage 可按实际测试对象替换，但必须是专门结论，per D-10、D-11）。</action>
  <acceptance_criteria>
    - `tests/indexVisibilitySummary.test.ts` asserts a success-only conclusion line beginning with `索引完成：`.
    - `tests/indexVisibilitySummary.test.ts` asserts a failure-only line containing `失败阶段:` and no success-only completion wording.
    - `tests/indexVisibilitySummary.test.ts` asserts a `跳过原因:` line with bucket labels and no default skipped file path list.
    - `tests/indexVisibilitySummary.test.ts` asserts the three explicit edge-case conclusions named in the task action.
    - `pnpm test -- tests/indexVisibilitySummary.test.ts` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/indexVisibilitySummary.test.ts</automated>
  </verify>
  <done>Final-summary expectations are locked before the CLI renderer changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement separate summary renderers with skip buckets and explicit edge-case conclusions</name>
  <read_first>
    src/index.ts
    tests/indexVisibilitySummary.test.ts
    src/scanner/index.ts
    .planning/phases/03-index-visibility/03-01-PLAN.md
    .planning/phases/03-index-visibility/03-RESEARCH.md
  </read_first>
  <files>src/index.ts</files>
  <behavior>
    - Success and failure follow distinct final templates.
    - Success summary includes skipped reason distribution when available.
    - Failure summary uses typed stage + partial stats and then preserves Phase 2 diagnostics if the underlying error is an embedding fatal.
  </behavior>
  <action>在 `src/index.ts` 中新增纯格式化 helper，把最终输出拆成 success/failure 两套模板（per D-07）。成功分支不要再只打印 `索引完成 (${duration}s)`；改为先生成专门结论行：常规成功用 `索引完成：索引已更新`，无可索引变更用 `索引完成：没有检测到新的可索引变更`，仅删除/自愈用 `索引完成：已同步删除或自愈，无新增向量嵌入`。随后输出持续时间和数值统计，并在 `stats.skipped > 0` 时增加 `跳过原因:` 行，把 `large_file`、`binary_file`、`ignored_json`、`no_indexable_chunks`、`processing_error` 映射为稳定中文标签。失败分支则输出独立模板：首行 `索引失败：...`，第二行 `失败阶段: {stage}`，第三行 `已知统计: 总数:{...} 新增:{...} 修改:{...} 未变:{...} 删除:{...} 跳过:{...} 错误:{...}`，若有 `skippedByReason` 再追加 `跳过原因:`。如果底层错误同时是 `EmbeddingFatalError`，保留 Phase 2 的 diagnostics block 继续在失败摘要之后逐行输出；不得让任何失败路径重新出现成功结论文案、`索引完成 (`、或 success-only stats 模板（per D-08、D-09）。</action>
  <acceptance_criteria>
    - `src/index.ts` contains separate helpers or branches for success and failure summary rendering.
    - `src/index.ts` contains the exact edge-case success strings `索引完成：没有检测到新的可索引变更` and `索引完成：已同步删除或自愈，无新增向量嵌入`.
    - `src/index.ts` failure branch emits `失败阶段:` and `已知统计:` before any Phase 2 diagnostics block.
    - `src/index.ts` contains a `跳过原因:` renderer using the five skip bucket keys from 03-01.
    - `src/index.ts` failure paths do not print `索引完成 (` or reuse the success-only statistics template.
    - `pnpm test -- tests/indexVisibilitySummary.test.ts && pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/indexVisibilitySummary.test.ts && pnpm build</automated>
  </verify>
  <done>The final CLI output is stage-honest, skip-aware, and explicit about edge cases while preserving earlier fail-fast and diagnostics contracts.</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/indexVisibilitySummary.test.ts`
- `pnpm build`
</verification>

<success_criteria>

- VIS-03 is true: success/failure state at the terminal matches the real indexing outcome.
- VIS-02 is visible in the final summary through aggregated skip reasons.
- Edge cases get explicit conclusions rather than being hidden inside generic templates or debug logs.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-index-visibility/03-03-SUMMARY.md`
</output>
