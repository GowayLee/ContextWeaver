---
phase: 02-provider-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/embedding.test.ts
  - tests/indexer/index.test.ts
  - src/api/embedding.ts
  - src/indexer/index.ts
autonomous: true
requirements:
  - DIAG-01
  - DIAG-03
must_haves:
  truths:
    - When embedding fails, the error object retains provider-aware diagnostics instead of only a plain string.
    - The system can distinguish authentication, rate limit, batch-too-large, dimension mismatch, timeout, network, incompatible response, and unknown embedding failures.
    - Indexer rethrows fatal embedding failures with stage context while preserving diagnostics for the CLI.
  artifacts:
    - path: tests/api/embedding.test.ts
      provides: Regression coverage for diagnostics capture and category classification.
    - path: tests/indexer/index.test.ts
      provides: Regression coverage for diagnostics preservation through indexer rethrow.
    - path: src/api/embedding.ts
      provides: Typed embedding diagnostics contract and conservative failure categorization.
    - path: src/indexer/index.ts
      provides: Fatal rethrow path that preserves diagnostics metadata.
  key_links:
    - from: src/api/embedding.ts
      to: src/indexer/index.ts
      via: `EmbeddingFatalError` carrying diagnostics through the batch/index boundary.
      pattern: diagnostics|EmbeddingFatalError
    - from: src/api/embedding.ts
      to: failure categories
      via: explicit classification helper for provider/network/response failures.
      pattern: authentication|rate_limit|batch_too_large|dimension_mismatch|timeout|network|incompatible_response|unknown
---

<objective>
Define the provider-diagnostics contract at the embedding and indexer layers.

Purpose: satisfy D-03 and D-04 first so later CLI rendering can display trustworthy provider-aware diagnostics without re-parsing ad hoc strings.
Output: regression tests plus API/indexer changes that preserve raw provider metadata, add safe request diagnostics, and classify common failure categories conservatively.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-diagnostics/02-CONTEXT.md
@.planning/phases/02-provider-diagnostics/02-RESEARCH.md
@.planning/phases/02-provider-diagnostics/02-VALIDATION.md
@.planning/phases/01-fail-fast-exit/01-01-SUMMARY.md
@.planning/phases/01-fail-fast-exit/01-02-SUMMARY.md

<interfaces>
From `src/api/embedding.ts` (current contract to extend):

```ts
export class EmbeddingFatalError extends Error {
  readonly stage = "embed";
}

export class EmbeddingClient {
  async embedBatch(
    texts: string[],
    batchSize?: number,
    onProgress?: (completed: number, total: number) => void,
  ): Promise<EmbeddingResult[]>;
}
```

Target contract this plan should establish for downstream plans:

```ts
export type EmbeddingFailureCategory =
  | "authentication"
  | "rate_limit"
  | "batch_too_large"
  | "dimension_mismatch"
  | "timeout"
  | "network"
  | "incompatible_response"
  | "unknown";

export interface EmbeddingFailureDiagnostics {
  stage: "embed";
  category: EmbeddingFailureCategory;
  httpStatus: number | null;
  providerType: string | null;
  providerCode: string | null;
  upstreamMessage: string;
  endpointHost: string;
  endpointPath: string;
  model: string;
  batchSize: number;
  dimensions: number;
  requestCount: number;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock provider diagnostics and category mapping with regression tests</name>
  <read_first>
    src/api/embedding.ts
    src/indexer/index.ts
    tests/api/embedding.test.ts
    tests/indexer/index.test.ts
    .planning/phases/02-provider-diagnostics/02-CONTEXT.md
    .planning/phases/02-provider-diagnostics/02-RESEARCH.md
  </read_first>
  <files>tests/api/embedding.test.ts, tests/indexer/index.test.ts</files>
  <behavior>
    - Test 1: per D-03 and DIAG-01, HTTP embedding failures preserve raw provider `type`, `code`, `status`, and upstream message in a diagnostics payload.
    - Test 2: per D-04 and DIAG-03, clear failures classify as `authentication`, `rate_limit`, `batch_too_large`, `dimension_mismatch`, `timeout`, `network`, and `incompatible_response`; ambiguous failures stay `unknown`.
    - Test 3: per DIAG-01, `Indexer.indexFiles()` rethrows an `EmbeddingFatalError` whose diagnostics survive the indexer wrapper and still indicate `stage: 'embed'`.
  </behavior>
  <action>扩展 `tests/api/embedding.test.ts` 与 `tests/indexer/index.test.ts`，先把 Phase 2 的诊断契约写成可执行断言。为 API 层至少覆盖 7 个明确分类样例：`401/403` 或 `auth/api_key/unauthorized/forbidden` -> `authentication`；`429` 或 `rate/quota/too many requests` -> `rate_limit`；`400/413` 且 message/code 含 `batch/too large/max input/payload too large` -> `batch_too_large`；任何 `dimension` 信号 -> `dimension_mismatch`；`AbortError`/`ETIMEDOUT`/`timeout` -> `timeout`；现有网络模式如 `ECONNRESET`/`ENOTFOUND`/`fetch failed` -> `network`；HTTP 成功但返回体缺少 `data`、embedding 非数组、索引越界或向量长度不等于 `dimensions` -> `incompatible_response`。同时断言 diagnostics 中包含 `stage`, `httpStatus`, `providerType`, `providerCode`, `upstreamMessage`, `endpointHost`, `endpointPath`, `model`, `batchSize`, `dimensions`, `requestCount`。indexer 测试要断言重抛后这些字段仍可读取，而不是只剩字符串消息。</action>
  <acceptance_criteria>
    - `tests/api/embedding.test.ts` contains assertions for all 8 categories: `authentication`, `rate_limit`, `batch_too_large`, `dimension_mismatch`, `timeout`, `network`, `incompatible_response`, `unknown`.
    - `tests/api/embedding.test.ts` asserts `httpStatus`, `providerType`, `providerCode`, `endpointHost`, `endpointPath`, `model`, `batchSize`, `dimensions`, and `requestCount` fields.
    - `tests/indexer/index.test.ts` asserts the rethrown fatal error still exposes diagnostics with `stage === 'embed'`.
    - `pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts</automated>
  </verify>
  <done>Regression tests precisely define the provider-aware diagnostics contract and category taxonomy for Phase 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed embedding diagnostics and preserve them through indexer rethrow</name>
  <read_first>
    src/api/embedding.ts
    src/indexer/index.ts
    tests/api/embedding.test.ts
    tests/indexer/index.test.ts
    src/config.ts
    .planning/phases/02-provider-diagnostics/02-RESEARCH.md
  </read_first>
  <files>src/api/embedding.ts, src/indexer/index.ts</files>
  <behavior>
    - `EmbeddingFatalError` carries a typed diagnostics object with raw provider fields and safe request context.
    - Failure categorization is conservative: only confident cases map to a standard category; otherwise `unknown`.
    - `Indexer.batchIndex()` keeps `向量嵌入阶段失败: ...` message context while rethrowing the same diagnostics-bearing fatal error.
  </behavior>
  <action>在 `src/api/embedding.ts` 中新增 `EmbeddingFailureCategory` 与 `EmbeddingFailureDiagnostics` 导出类型，并把 `EmbeddingFatalError` 扩展为可接收 `diagnostics`。在 `processBatch()` 中，解析 `this.config.baseUrl` 生成 `endpointHost` 与 `endpointPath`；从当前请求捕获 `model`, `batchSize`, `dimensions`, `requestCount`；对 HTTP 失败读取 `response.status` 与 provider `{ error: { type, code, message } }`；对网络/超时/响应格式错误构造同一套 diagnostics。实现明确分类 helper，规则必须写死在代码里，不要只写“按 message 判断”。对 `response.ok === true` 但 `data` 缺失、不是数组、索引非法、embedding 不是 number 数组、或 embedding 长度不等于 `this.config.dimensions` 的情况，抛出 `incompatible_response` fatal。随后在 `src/indexer/index.ts` 中，catch embedding fatal 时先 `clearVectorIndexHash(...)`，再重抛一个仍携带原 diagnostics 的 `EmbeddingFatalError`，message 保持 `向量嵌入阶段失败: ${upstreamMessage}`。不要在这里重新分类，也不要丢失 `httpStatus`/`providerType`/`providerCode` 等字段。</action>
  <acceptance_criteria>
    - `src/api/embedding.ts` exports `EmbeddingFailureCategory` and `EmbeddingFailureDiagnostics`.
    - `src/api/embedding.ts` constructs diagnostics containing `endpointHost`, `endpointPath`, `model`, `batchSize`, `dimensions`, and `requestCount`.
    - `src/api/embedding.ts` contains explicit category strings for all 8 Phase 2 categories.
    - `src/api/embedding.ts` validates successful-looking responses and throws `incompatible_response` on malformed payloads or dimension-length mismatch.
    - `src/indexer/index.ts` rethrows a diagnostics-bearing `EmbeddingFatalError` after `clearVectorIndexHash(`.
    - `pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts</automated>
  </verify>
  <done>Embedding/indexer layers now emit provider-aware, safe, and classifiable fatal diagnostics that later plans can render directly.</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts`
- `pnpm build`
</verification>

<success_criteria>

- DIAG-03 is executable through tests and implementation, including all required standard categories plus `unknown` fallback.
- DIAG-01 groundwork exists: fatal embedding errors preserve provider-aware metadata instead of only string messages.
- Indexer keeps Phase 1 fail-fast semantics while forwarding diagnostics for user-visible rendering.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-provider-diagnostics/02-01-SUMMARY.md`
</output>
