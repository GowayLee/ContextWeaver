---
phase: 02-provider-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/indexCli.test.ts
  - src/index.ts
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
must_haves:
  truths:
    - When `index` fails in the embedding stage, the terminal first shows a concise failure verdict and then a readable diagnostics block.
    - The diagnostics block shows safe request context including endpoint host/path, model, batch size, dimensions, request count, HTTP status, and provider raw fields.
    - No API key, token, or Authorization header is exposed in default CLI output.
  artifacts:
    - path: tests/indexCli.test.ts
      provides: Regression coverage for dual-layer CLI diagnostics and secret redaction.
    - path: src/index.ts
      provides: Final failure renderer that prints the dual-layer diagnostics block.
  key_links:
    - from: src/index.ts
      to: src/api/embedding.ts
      via: rendering `EmbeddingFatalError.diagnostics` at the CLI boundary.
      pattern: diagnostics|索引失败
    - from: src/index.ts
      to: terminal output safety
      via: formatted block that excludes secrets while including safe request summary fields.
      pattern: endpoint|model|batch|dimensions|request
---

<objective>
Render Phase 2 provider diagnostics at the user-visible CLI boundary.

Purpose: implement D-01, D-02, D-05, D-06, and D-07 so `cw index` failure output becomes immediately diagnosable without requiring logs and without leaking secrets.
Output: CLI tests plus a failure renderer that prints an honest verdict followed by a safe multiline diagnostics block.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-diagnostics/02-CONTEXT.md
@.planning/phases/02-provider-diagnostics/02-RESEARCH.md
@.planning/phases/02-provider-diagnostics/02-VALIDATION.md
@.planning/phases/02-provider-diagnostics/02-01-PLAN.md
@.planning/phases/01-fail-fast-exit/01-02-SUMMARY.md

<interfaces>
From the diagnostics contract established in `02-01`:

```ts
export interface EmbeddingFailureDiagnostics {
  stage: "embed";
  category: EmbeddingFailureCategory;
  httpStatus: number | null;
  providerType: string | null;
  providerCode: string | null;
  upstreamMessage: string;
  endpointHost: string;
  endpointPath: string;
  model: string;
  batchSize: number;
  dimensions: number;
  requestCount: number;
}

export class EmbeddingFatalError extends Error {
  readonly stage = "embed";
  readonly diagnostics?: EmbeddingFailureDiagnostics;
}
```

Current CLI boundary to update:

```ts
export async function runIndexCliCommand(options: {
  rootPath: string;
  force?: boolean;
  yes?: boolean;
  isInteractive?: boolean;
  runIndexCommandFn?: typeof runIndexCommand;
  logger?: {
    info: (message: string) => void;
    error: (message: string) => void;
  };
  exit?: (code: number) => void;
}): Promise<void>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI regression tests for dual-layer provider diagnostics</name>
  <read_first>
    tests/indexCli.test.ts
    src/index.ts
    .planning/phases/02-provider-diagnostics/02-CONTEXT.md
    .planning/phases/02-provider-diagnostics/02-01-PLAN.md
  </read_first>
  <files>tests/indexCli.test.ts</files>
  <behavior>
    - Test 1: per D-01/D-02 and DIAG-01, embedding fatal failure prints a first-line failure verdict plus additional diagnostics lines.
    - Test 2: per D-05/D-06 and DIAG-02, diagnostics include `stage`, `category`, `http status`, `endpoint host/path`, `model`, `batch size`, `dimensions`, `request items`, and raw provider `type/code/message`.
    - Test 3: per D-07, output never contains API key, `Bearer `, or raw Authorization header values.
  </behavior>
  <action>扩展 `tests/indexCli.test.ts`，构造一个带完整 diagnostics 的 `EmbeddingFatalError` 或等价错误对象，通过 `runIndexCliCommand()` 的失败分支触发渲染。断言第一行仍是 `索引失败: 向量嵌入阶段失败: ...`，随后存在多行诊断输出，至少覆盖 `阶段: embed`、`错误类别:`、`HTTP 状态:`、`Provider type:`、`Provider code:`、`Provider message:`、`Endpoint:`、`Model:`、`Batch size:`、`Dimensions:`、`Request items:`。再加入安全断言：任何输出都不能包含 `test-key`、`Bearer `、`Authorization`，也不能把完整 base URL 中的敏感查询串原样打印。成功路径现有断言必须继续保留，避免 Phase 2 破坏 Phase 1 的 success-only contract。</action>
  <acceptance_criteria>
    - `tests/indexCli.test.ts` contains assertions for a first-line `索引失败:` verdict and subsequent diagnostics lines.
    - `tests/indexCli.test.ts` asserts presence of `阶段: embed`, `错误类别:`, `HTTP 状态:`, `Endpoint:`, `Model:`, `Batch size:`, `Dimensions:`, and `Request items:`.
    - `tests/indexCli.test.ts` asserts absence of `test-key`, `Bearer `, and `Authorization`.
    - `pnpm test -- tests/indexCli.test.ts` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/indexCli.test.ts</automated>
  </verify>
  <done>CLI expectations for readable diagnostics and secret safety are locked down in regression tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render safe multiline diagnostics in the top-level CLI failure path</name>
  <read_first>
    src/index.ts
    tests/indexCli.test.ts
    src/api/embedding.ts
    .planning/phases/02-provider-diagnostics/02-RESEARCH.md
  </read_first>
  <files>src/index.ts</files>
  <behavior>
    - Failure path prints one concise verdict line first, then a multiline diagnostics block.
    - The diagnostics block uses the typed `EmbeddingFatalError.diagnostics` contract directly and does not reclassify errors.
    - Safe fields are shown by default and secret fields are never rendered.
  </behavior>
  <action>在 `src/index.ts` 中新增一个纯格式化 helper（例如 `formatEmbeddingFailureDiagnostics(error)`），专门把 `EmbeddingFatalError.diagnostics` 转成多行字符串数组。输出顺序固定为：`阶段`、`错误类别`、`HTTP 状态`、`Provider type`、`Provider code`、`Provider message`、`Endpoint`、`Model`、`Batch size`、`Dimensions`、`Request items`。没有值时统一渲染 `<unknown>` 或 `<none>`，不要省略字段。`Endpoint` 只显示 `endpointHost + endpointPath`，不要输出 api key、Authorization header、原始 body、或任何 query string。然后在 `runIndexCliCommand()` catch 分支中保留第一行 `索引失败: ...`，再逐行 `output.error(...)` 打印诊断块；如果错误没有 diagnostics，则保持现有单行失败输出。成功分支与退出码行为不要改动。</action>
  <acceptance_criteria>
    - `src/index.ts` contains a formatting helper for diagnostics block output.
    - `src/index.ts` failure branch prints the original `索引失败:` verdict before any diagnostics lines.
    - `src/index.ts` renders all safe diagnostics fields even when some values are null, using `<unknown>` or `<none>` placeholders.
    - `src/index.ts` does not render `apiKey`, `Authorization`, or `Bearer ` anywhere in the diagnostics helper.
    - `pnpm test -- tests/indexCli.test.ts && pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/indexCli.test.ts && pnpm build</automated>
  </verify>
  <done>The user-visible CLI now shows a readable, safe, and provider-aware diagnostics block on embedding failures.</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/indexCli.test.ts`
- `pnpm build`
</verification>

<success_criteria>

- DIAG-01 is true at the terminal boundary: user sees provider-aware failure details, stage, status, and upstream summary.
- DIAG-02 is true at the terminal boundary: safe request summary is visible by default and secrets remain hidden.
- Phase 1 success-path output contract remains unchanged.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-provider-diagnostics/02-02-SUMMARY.md`
</output>
