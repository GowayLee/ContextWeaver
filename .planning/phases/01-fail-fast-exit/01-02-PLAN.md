---
phase: 01-fail-fast-exit
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/indexCli.test.ts
  - src/scanner/index.ts
  - src/index.ts
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - When `index` fails in the embedding stage, the CLI exits non-zero and prints a failure verdict with stage context.
    - When `index` fails, the user never sees `索引完成`, success statistics, or other success-only completion text.
    - Success-path completion logs still appear only for truly successful indexing runs.
  artifacts:
    - path: tests/indexCli.test.ts
      provides: Regression coverage for CLI failure verdict and success-output suppression.
    - path: src/scanner/index.ts
      provides: Fatal path that skips final `索引完成` progress callback.
    - path: src/index.ts
      provides: Final CLI success/failure output contract.
  key_links:
    - from: src/scanner/index.ts
      to: src/index.ts
      via: thrown fatal error with stage context instead of terminal success progress.
      pattern: onProgress\?\(100, 100, '索引完成'\)
    - from: src/index.ts
      to: terminal output
      via: success-only `logger.info` and failure-only `logger.error` branches.
      pattern: 索引完成|索引失败
---

<objective>
Wire Phase 1 fail-fast behavior into the scanner and CLI output contract.

Purpose: implement D-05 through D-08 so SAFE-02 is true at the user-visible boundary.
Output: regression tests and CLI/scanner changes that emit one honest failure verdict and suppress all misleading success completion output.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fail-fast-exit/01-CONTEXT.md
@.planning/phases/01-fail-fast-exit/01-RESEARCH.md
@.planning/phases/01-fail-fast-exit/01-VALIDATION.md
@.planning/phases/01-fail-fast-exit/01-01-PLAN.md

<interfaces>
From `src/scanner/index.ts`:

```ts
export interface ScanStats {
  totalFiles: number;
  added: number;
  modified: number;
  unchanged: number;
  deleted: number;
  skipped: number;
  errors: number;
  vectorIndex?: {
    indexed: number;
    deleted: number;
    errors: number;
  };
}

export type ProgressCallback = (
  current: number,
  total?: number,
  message?: string,
) => void;

export async function scan(
  rootPath: string,
  options?: ScanOptions,
): Promise<ScanStats>;
```

From `src/cli.ts` and `src/index.ts`:

```ts
export async function runIndexCommand(options: {
  rootPath: string;
  force?: boolean;
  yes?: boolean;
  isInteractive?: boolean;
  scanFn?: (rootPath: string, options: ScanOptions) => Promise<ScanStats>;
}): Promise<ScanStats>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI regression tests for honest fatal-exit output</name>
  <read_first>
    tests/indexCli.test.ts
    src/cli.ts
    src/index.ts
    src/scanner/index.ts
    .planning/phases/01-fail-fast-exit/01-CONTEXT.md
  </read_first>
  <files>tests/indexCli.test.ts</files>
  <behavior>
    - Test 1: per D-05 and D-06, a fatal embedding-stage failure produces a failure verdict and does not produce `索引完成` or success statistics.
    - Test 2: per D-07, progress logs before the failure may remain, but there is no final success summary on the fatal path.
    - Test 3: success-path behavior remains unchanged when scan succeeds.
  </behavior>
  <action>扩展 `tests/indexCli.test.ts`，新增围绕 Phase 1 CLI 契约的回归测试。至少覆盖两类场景：一是 `scanFn` 抛出带有向量嵌入阶段语义的 fatal error 时，顶层命令失败分支只输出失败结论与阶段上下文，不输出 `索引完成`、`总数:` 这类成功统计；二是真正成功时，原有成功路径仍保留。测试要直接断言日志字符串，避免实现把失败包装成模糊错误后又被成功日志覆盖。</action>
  <acceptance_criteria>
    - `tests/indexCli.test.ts` contains a fatal-path case asserting absence of `索引完成`.
    - `tests/indexCli.test.ts` contains a fatal-path case asserting absence of `总数:` success statistics.
    - `tests/indexCli.test.ts` contains a fatal-path case asserting presence of stage-context failure wording.
    - `pnpm test -- tests/indexCli.test.ts` fails before Task 2 implementation and passes after it.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/indexCli.test.ts</automated>
  </verify>
  <done>CLI output contract is locked down by tests for both fatal and successful runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suppress scanner success completion and align top-level CLI verdict</name>
  <read_first>
    src/scanner/index.ts
    src/index.ts
    tests/indexCli.test.ts
    .planning/phases/01-fail-fast-exit/01-RESEARCH.md
  </read_first>
  <files>src/scanner/index.ts, src/index.ts</files>
  <behavior>
    - Fatal indexing failure skips final `索引完成` progress emission from scanner.
    - Top-level CLI prints success logs only on success and failure verdict + stage context on fatal failure.
    - Process exits non-zero on fatal failure and success statistics are never printed on that path.
  </behavior>
  <action>在 `src/scanner/index.ts` 中调整 fatal 传播路径：如果下游 indexer/embedding 抛出 fatal 错误，直接把错误向上抛出，并确保 `options.onProgress?.(100, 100, '索引完成')` 只出现在真正成功路径。随后在 `src/index.ts` 中收紧 `index` 命令的出口契约：只有 `runIndexCommand(...)` 成功返回时才打印 `索引完成 (${duration}s)` 与 `总数:${...}` 成功统计；捕获 fatal 失败时，输出明确失败结论和阶段上下文（按 D-05），并保持 `process.exit(1)`。失败路径禁止打印任何 success-only 文案，包括 `索引完成`、成功统计和完成摘要；但允许保留失败前已发生的阶段进度日志，符合 D-07。</action>
  <acceptance_criteria>
    - `src/scanner/index.ts` only emits `索引完成` from the true success path.
    - `src/index.ts` keeps `logger.info(` success summary lines inside the success branch only.
    - `src/index.ts` failure branch logs stage-context failure wording and still calls `process.exit(1)`.
    - `pnpm test -- tests/indexCli.test.ts` exits 0.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/indexCli.test.ts && pnpm build</automated>
  </verify>
  <done>User-visible CLI output is honest: success text appears only on success, and fatal embedding failure ends with a non-zero failure verdict.</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/indexCli.test.ts`
- `pnpm build`
</verification>

<success_criteria>

- SAFE-02 is covered by executable tests and implementation instructions.
- Fatal embedding failure never ends with `索引完成`, success statistics, or similar misleading completion text.
- Successful indexing output remains intact for the non-failing path.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-fail-fast-exit/01-02-SUMMARY.md`
</output>
