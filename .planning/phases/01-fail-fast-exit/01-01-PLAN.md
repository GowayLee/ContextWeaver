---
phase: 01-fail-fast-exit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/embedding.test.ts
  - tests/indexer/index.test.ts
  - src/api/embedding.ts
  - src/indexer/index.ts
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - User can rely on `index` to stop scheduling further embedding work after the first fatal post-retry embedding failure.
    - In-flight embedding requests are best-effort cancelled and any late results are ignored.
    - Fatal embedding failure reaches the indexer as a thrown failure, not a soft success-looking stats object.
  artifacts:
    - path: tests/api/embedding.test.ts
      provides: Regression coverage for shared fatal embedding session behavior.
    - path: tests/indexer/index.test.ts
      provides: Regression coverage for indexer fatal propagation and hash clearing.
    - path: src/api/embedding.ts
      provides: Shared fatal session, best-effort abort, and late-result discard logic.
    - path: src/indexer/index.ts
      provides: Fatal embedding error propagation instead of soft stats fallback.
  key_links:
    - from: src/api/embedding.ts
      to: src/indexer/index.ts
      via: Embedding fatal error thrown from `embedBatch()` into `batchIndex()`.
      pattern: embedBatch\(|Embedding 失败
    - from: src/indexer/index.ts
      to: SQLite vector hash state
      via: `clearVectorIndexHash(...)` on fatal failure before rethrow.
      pattern: clearVectorIndexHash
---

<objective>
Define and implement the fail-fast embedding contract for Phase 1.

Purpose: enforce D-01 through D-04 and satisfy SAFE-01 at the lowest reliable layer before changing CLI wording.
Output: regression tests plus embedding/indexer code that aborts future work, drops late results, and throws a fatal failure upward.
</objective>

<execution_context>
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/workflows/execute-plan.md
@/home/haurynlee/00-workspace/engineering/ContextWeaver/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fail-fast-exit/01-CONTEXT.md
@.planning/phases/01-fail-fast-exit/01-RESEARCH.md
@.planning/phases/01-fail-fast-exit/01-VALIDATION.md

<interfaces>
From `src/api/embedding.ts`:

```ts
export interface EmbeddingResult {
  text: string;
  embedding: number[];
  index: number;
}

export class EmbeddingClient {
  async embedBatch(
    texts: string[],
    batchSize?: number,
    onProgress?: (completed: number, total: number) => void,
  ): Promise<EmbeddingResult[]>;
}
```

From `src/indexer/index.ts`:

```ts
export interface IndexStats {
  indexed: number;
  deleted: number;
  errors: number;
  skipped: number;
}

export class Indexer {
  async indexFiles(
    db: Database.Database,
    results: ProcessResult[],
    onProgress?: (indexed: number, total: number) => void,
  ): Promise<IndexStats>;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-fast regression tests for embedding and indexer</name>
  <read_first>
    src/api/embedding.ts
    src/indexer/index.ts
    tests/indexCli.test.ts
    .planning/phases/01-fail-fast-exit/01-CONTEXT.md
  </read_first>
  <files>tests/api/embedding.test.ts, tests/indexer/index.test.ts</files>
  <behavior>
    - Test 1: per D-03, once one embedding batch fails fatally after the existing retries, later unstarted batches never begin.
    - Test 2: per D-04, late successful responses after fatal state do not call progress callbacks and do not contribute returned embeddings.
    - Test 3: per D-03 and SAFE-01, `Indexer.indexFiles()` rethrows the fatal embedding failure after clearing `vector_index_hash` instead of returning only error stats.
  </behavior>
  <action>创建 `tests/api/embedding.test.ts` 和 `tests/indexer/index.test.ts`。在测试里直接覆盖当前 Phase 1 契约：保留 429/网络错误重试（D-01），但对重试后仍失败的普通 embedding 错误视为 fatal；fatal 发生后未启动批次不得继续调用底层请求；已启动但晚到的成功结果必须被丢弃，不能推进 `onProgress`；`Indexer.indexFiles()` 在 embedding fatal 时必须调用 `clearVectorIndexHash(...)` 并抛出带有向量嵌入阶段语义的错误，而不是返回成功样式统计。测试名称和断言要直接包含这些语义，保证后续实现不能偷换成“只记 errors++”。</action>
  <acceptance_criteria>
    - `tests/api/embedding.test.ts` contains a case covering unstarted batches not beginning after a fatal failure.
    - `tests/api/embedding.test.ts` contains a case covering late successful results being discarded without progress advancement.
    - `tests/indexer/index.test.ts` contains `clearVectorIndexHash` assertions for fatal embedding failure.
    - `pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts</automated>
  </verify>
  <done>Focused regression tests exist and fully describe the fatal embedding contract for Phase 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared fatal embedding state and fatal propagation</name>
  <read_first>
    src/api/embedding.ts
    src/indexer/index.ts
    tests/api/embedding.test.ts
    tests/indexer/index.test.ts
    .planning/phases/01-fail-fast-exit/01-RESEARCH.md
  </read_first>
  <files>src/api/embedding.ts, src/indexer/index.ts</files>
  <behavior>
    - Fatal post-retry embedding failure flips shared session state and blocks not-yet-started batches.
    - In-flight requests are best-effort aborted and late successes are ignored.
    - Indexer clears vector hash state and rethrows fatal embedding failure upward, without logging `向量索引完成` on that path.
  </behavior>
  <action>在 `src/api/embedding.ts` 内为单次 `embedBatch()` 引入共享 fatal session（不要改动全局 rate-limit controller 的 429/网络重试职责，遵守 D-01、D-02）。用 `AbortController` 绑定每个 `fetch()` 请求；当首个重试后仍失败的 embedding 错误出现时，记录 fatal 状态、best-effort abort 已发出的请求，并在每个批次启动前检查 fatal 状态以阻止尚未开始的批次。对 fatal 之后才返回的成功响应，在写入 `progress.recordBatch(...)` 和返回 embedding 前丢弃。随后在 `src/indexer/index.ts` 中，把 embedding fatal 从“记录日志后返回 `{ success: 0, errors: files.length }`”改成“清理本轮 `vector_index_hash` 后抛出带 `embed` 阶段上下文的错误”；fatal 路径不得再输出 `向量索引完成`。保留其余成功路径日志与返回结构不变。</action>
  <acceptance_criteria>
    - `src/api/embedding.ts` contains `AbortController` usage for batch requests.
    - `src/api/embedding.ts` contains a per-`embedBatch` fatal-state guard checked before starting new work.
    - `src/api/embedding.ts` prevents `progress.recordBatch(` from running for late results after fatal state.
    - `src/indexer/index.ts` rethrows fatal embedding failure after `clearVectorIndexHash(`.
    - `src/indexer/index.ts` no longer logs `向量索引完成` when the fatal embedding path is taken.
    - `pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts</automated>
  </verify>
  <done>Embedding/indexer layers now enforce fail-fast semantics and throw a fatal failure upstream exactly once.</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/api/embedding.test.ts tests/indexer/index.test.ts`
- `pnpm build`
</verification>

<success_criteria>

- SAFE-01 is covered by executable tests and implementation.
- No new embedding batches start after the first fatal post-retry failure.
- Fatal embedding failure leaves indexer on a thrown failure path, not a success-looking stats path.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-fail-fast-exit/01-01-SUMMARY.md`
</output>
