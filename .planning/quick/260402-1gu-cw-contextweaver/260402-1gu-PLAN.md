---
mode: quick
id: 260402-1gu
objective: 修复全局安装后 cw/contextweaver 因入口守卫误判而无输出的问题
files_modified:
  - src/index.ts
  - tests/entryCli.test.ts
autonomous: true
must_haves:
  truths:
    - "全局安装场景下通过 bin 别名执行 cw 或 contextweaver 时，CLI 会进入统一入口而不是静默退出"
    - "无参数执行时仍能稳定输出帮助信息"
    - "两个发布别名在全局包装/符号链接场景下行为一致"
  artifacts:
    - path: "src/index.ts"
      provides: "对全局 bin/symlink 安全的入口执行判断"
    - path: "tests/entryCli.test.ts"
      provides: "覆盖全局安装别名入口的回归 smoke test"
  key_links:
    - from: "package.json bin -> dist/index.js"
      to: "src/index.ts -> runCli()"
      via: "全局安装生成的包装脚本/符号链接调用"
      pattern: "入口判断不能只依赖 path.resolve(process.argv[1]) 与 import.meta.url 的字面相等"
---

<objective>
定位并修复全局安装后 `cw` / `contextweaver` 无输出的问题。

Purpose: 保证 npm 全局安装产物按既有 CLI 形态正常启动，避免用户执行入口命令时静默失败。
Output: 更稳健的 CLI 入口守卫 + 能复现并锁定该问题的回归测试。
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@package.json
@src/index.ts
@tests/entryCli.test.ts

<interfaces>
From package.json:
```json
"bin": {
  "contextweaver": "dist/index.js",
  "cw": "dist/index.js"
}
```

From src/index.ts:

```ts
export function runCli(
  argv = process.argv.slice(2),
  invokedPath = process.argv[1],
): void;
```

Current entry guard:

```ts
if (
  process.argv[1] &&
  path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)
) {
  runCli();
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修正全局 bin/symlink 场景下的 CLI 入口判定</name>
  <files>src/index.ts, tests/entryCli.test.ts</files>
  <behavior>
    - Test 1: 通过模拟全局安装包装脚本或符号链接执行 `cw`/`contextweaver` 且不带参数时，进程退出码为 0，并输出 help。
    - Test 2: 两个别名在该场景下输出一致，不再出现空字符串。
    - Test 3: 直接执行 `dist/index.js` 的现有 help/version 行为保持不变。
  </behavior>
  <action>先在 `tests/entryCli.test.ts` 增加一个能复现“wrapper path 与 import.meta.url 不相等但仍应执行入口”的 smoke regression；再更新 `src/index.ts` 的入口守卫，改为基于 realpath/可执行入口解析或等价的稳健方式判断当前模块是否作为主程序运行。不要改动命令定义、帮助文本或 `runCli` 的公开签名；只修正“全局包装脚本/符号链接调用时未进入 runCli”这一根因。</action>
  <verify>
    <automated>pnpm test -- tests/entryCli.test.ts</automated>
  </verify>
  <done>新增测试先失败后通过；全局别名包装场景下不再静默退出，现有 `--help`/`--version` 行为不回归。</done>
</task>

<task type="auto">
  <name>Task 2: 做一次发布入口级别的快速回归验证</name>
  <files>tests/entryCli.test.ts</files>
  <action>补充或整理入口 smoke 断言，确保覆盖三类代表性路径：直接执行 `dist/index.js`、通过 `contextweaver` 包装调用、通过 `cw` 包装调用。验证重点是“有输出 + 状态码正确 + 两个别名一致”，不是扩展新功能。</action>
  <verify>
    <automated>pnpm test -- tests/entryCli.test.ts && pnpm test -- tests/indexCli.test.ts</automated>
  </verify>
  <done>入口相关测试能同时证明修复生效且未破坏已有 CLI 分发别名与基础输出语义。</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/entryCli.test.ts` 通过，且新增用例能覆盖全局安装包装/符号链接入口。
- `pnpm test -- tests/indexCli.test.ts` 通过，确认入口修复未影响索引命令边界输出。
</verification>

<success_criteria>

- 全局安装后的 `cw` 与 `contextweaver` 执行不再出现“无输出”。
- 无参数执行能显示 help；`--version` 仍只输出版本号。
- 修复范围仅限入口判定与相关测试，不改变既有 CLI 命令面。
  </success_criteria>

<output>
实施后在同目录补充执行结果摘要（如工作流要求）。
</output>
